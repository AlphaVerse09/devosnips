
"use client";

// This component is no longer used and can be deleted.
// Its functionality has been moved to:
// - src/components/layout/main-category-nav.tsx (for main categories in sidebar)
// - src/components/snippets/subcategory-nav.tsx (for sub-categories in main content area)

// Keeping the file for now to avoid breaking imports if any linger,
// but it should be removed in a cleanup phase.
// If you are sure no other file imports this, you can delete it.

export function CategoryFilter() {
  return null; 
}
